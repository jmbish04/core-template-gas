# Workspace Admin

This is the starter project that demonstrates the repo contract:

- server code in `src/server`
- React client in `src/client`
- shared Workspace and Cloudflare helpers imported from `shared/`
- deployment driven by the root registry and root scripts

The project is intentionally generic so it can evolve into a real web app, add-on, or automation console.
