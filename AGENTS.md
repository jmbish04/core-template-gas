{"findings": [{"body": "The existing AGENTS.md is a solid foundation but is missing several project-level constraints that are visible in the repository configuration: Node/Deno runtime expectations, the Prettier/ESLint/editorconfig toolchain, the exact npm script surface, the role of `appsscript.deno.json`/`deno.jsonc`, the requirement to keep tests passing, and the expectation that formatting/linting/type-checking are part of the agent workflow. It also does not explicitly call out the `shared/` directory (referenced in README) or the `scripts/` directory, and it could strengthen the appsscript.json scope mapping and the worker-pairing rule." , "code_location": "AGENTS.md", "confidence_score": 0.95, "priority": 1, "title": "AGENTS.md is incomplete relative to repo tooling and structure"}, {"body": "The repository uses Node >=20, ESM (`\"type\": \"module\"`), React 19, Deno tasks, esbuild-based build scripts, Prettier, ESLint, and `tsc --noEmit`. None of these are reflected in the agent instructions, which means future agents may generate CommonJS, skip linting, or use incompatible package managers.", "code_location": "package.json, deno.jsonc, eslint.config.mjs, .prettierrc.json, .editorconfig", "confidence_score": 0.95, "priority": 1, "title": "Runtime and quality toolchain is undocumented for agents"}, {"body": "The README describes `shared/` and `scripts/` as first-class directories, but the current AGENTS.md only mentions `shared/` in passing and never mentions `scripts/`. Agents need to know that all build/deploy logic is centralized in `scripts/` and that shared code belongs in `shared/`.", "code_location": "README.md, AGENTS.md", "confidence_score": 0.9, "priority": 2, "title": "shared/ and scripts/ directories need explicit contract"}, {"body": "The `appsscript.json` scope mapping is useful but can be made more explicit and include additional services such as `PropertiesService`, `ScriptApp`, UI methods, `MailApp`, `GmailApp`, and `UrlFetchApp`. It should also state that scopes must be verified after any shared-library change.", "code_location": "AGENTS.md", "confidence_score": 0.85, "priority": 2, "title": "OAuth scope mapping can be expanded and hardened"}, {"body": "There is no instruction about keeping tests green or running `npm run validate`, `npm run typecheck`, `npm run lint`, and `npm run format` before considering work complete. Adding this will reduce broken CI cycles.", "code_location": "package.json, main.test.ts", "confidence_score": 0.9, "priority": 1, "title": "Testing and validation workflow missing from agent instructions"}] , "overall_confidence_score": 0.92, "overall_correctness": "patch is correct", "overall_explanation": "The repository's AGENTS.md should be updated to include the full runtime/toolchain contract, the shared/scripts directory rules, an expanded OAuth scope mapping, and explicit validation/formatting requirements. The proposed patch preserves the existing structure while making the instructions comprehensive enough for future agents to operate safely in this multi-project Google Apps Script workspace."}